import PropTypes from 'prop-types'
import React, { Component } from 'react'

export class ScoreCard extends Component {
  static propTypes = {}

  render() {
    return (
      <div>
        This is ScoreCard item
      </div>
    )
  }
}

export default ScoreCard